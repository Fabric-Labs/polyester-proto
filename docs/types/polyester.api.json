{
  "types": [
    {
      "name": "polyester.api.AuthenticationMethod",
      "kind": "enum",
      "doc": "AuthenticationMethod identifies a credential type accepted by an RPC.",
      "enum": {
        "values": [
          {
            "name": "SESSION_TOKEN",
            "number": 1,
            "doc": "A bearer token for an interactive user session."
          },
          {
            "name": "API_KEY",
            "number": 2,
            "doc": "A signed API key request."
          }
        ]
      }
    },
    {
      "name": "polyester.api.MFARequirement",
      "kind": "enum",
      "doc": "MFARequirement describes the interactive MFA assurance documented for an RPC.",
      "enum": {
        "values": [
          {
            "name": "MFA_RECENT",
            "number": 1,
            "doc": "The RPC requires recently completed interactive MFA. Stale assurance must be renewed."
          },
          {
            "name": "MFA_FRESH_STEP_UP",
            "number": 2,
            "doc": "The RPC always requires a fresh, single-use step-up proof, even when MFA was completed recently."
          },
          {
            "name": "MFA_CONDITIONAL",
            "number": 3,
            "doc": "The required MFA level depends on the request."
          }
        ]
      }
    }
  ]
}
