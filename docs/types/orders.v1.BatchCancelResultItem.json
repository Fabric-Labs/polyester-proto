{
  "types": [
    {
      "name": "orders.v1.BatchCancelResultItem.Status",
      "kind": "enum",
      "doc": "Per-item cancellation outcome.",
      "enum": {
        "values": [
          {
            "name": "ACCEPTED",
            "number": 1,
            "doc": "The cancellation request was accepted for processing."
          },
          {
            "name": "REJECTED",
            "number": 2,
            "doc": "The cancellation request was rejected."
          }
        ]
      }
    }
  ]
}
